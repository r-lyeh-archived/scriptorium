#!/bin/sh

# Run this script in the S9 root to rebuild the help symlinks

cd help || exit 1

ln -fs amb amb-collector
ln -fs amb amb-donep
ln -fs amb amb-reset
ln -fs assq assoc
ln -fs assq assv
ln -fs bit0 bitstar
ln -fs bit0 bitstarc
ln -fs bit0 bita
ln -fs bit0 bitcstar
ln -fs bit0 bitb
ln -fs bit0 bitneq
ln -fs bit0 bitplus
ln -fs bit0 bitnplus
ln -fs bit0 biteq
ln -fs bit0 bitcb
ln -fs bit0 bitplusc
ln -fs bit0 bitca
ln -fs bit0 bitcplus
ln -fs bit0 bitnstar
ln -fs bit0 bit1
ln -fs bit0 bitsl
ln -fs bit0 bitsr
ln -fs bitwise-and bitwise-1
ln -fs bitwise-and bitwise-2
ln -fs bitwise-and bitwise-and-c1
ln -fs bitwise-and bitwise-and-c2
ln -fs bitwise-and bitwise-and-not
ln -fs bitwise-and bitwise-c1
ln -fs bitwise-and bitwise-c2
ln -fs bitwise-and bitwise-clear
ln -fs bitwise-and bitwise-or
ln -fs bitwise-and bitwise-or-c1
ln -fs bitwise-and bitwise-or-c2
ln -fs bitwise-and bitwise-or-not
ln -fs bitwise-and bitwise-set
ln -fs bitwise-and bitwise-shift-left
ln -fs bitwise-and bitwise-shift-right
ln -fs bitwise-and bitwise-xor
ln -fs bitwise-and bitwise-xor-not
ln -fs caar caaaar
ln -fs caar caaadr
ln -fs caar caaar
ln -fs caar caadar
ln -fs caar caaddr
ln -fs caar caadr
ln -fs caar cadaar
ln -fs caar cadadr
ln -fs caar cadar
ln -fs caar caddar
ln -fs caar cadddr
ln -fs caar caddr
ln -fs caar cadr
ln -fs caar cdaaar
ln -fs caar cdaadr
ln -fs caar cdaar
ln -fs caar cdadar
ln -fs caar cdaddr
ln -fs caar cdadr
ln -fs caar cdar
ln -fs caar cddaar
ln -fs caar cddadr
ln -fs caar cddar
ln -fs caar cdddar
ln -fs caar cddddr
ln -fs caar cdddr
ln -fs caar cddr
ln -fs call-with-current-continuation callslashcc
ln -fs call-with-input-file call-with-output-file
ln -fs catch throw
ln -fs char-alphabeticp char-lower-casep
ln -fs char-alphabeticp char-numericp
ln -fs char-alphabeticp char-upper-casep
ln -fs char-alphabeticp char-whitespacep
ln -fs char-cieqp char-cigep
ln -fs char-cieqp char-cigtp
ln -fs char-cieqp char-cilep
ln -fs char-cieqp char-ciltp
ln -fs char-to-integer integer-to-char
ln -fs char-upcase char-downcase
ln -fs chareqp chargep
ln -fs chareqp chargtp
ln -fs chareqp charlep
ln -fs chareqp charltp
ln -fs close-input-port close-output-port
ln -fs combine combinestar
ln -fs complement compose
ln -fs complement const
ln -fs complement curry
ln -fs complement curryr
ln -fs complement fork
ln -fs current-input-port current-output-port
ln -fs curs_addch curs_addstr
ln -fs curs_addch curs_beep
ln -fs curs_addch curs_flash
ln -fs curs_addch curs_move
ln -fs curs_addch curs_mvaddch
ln -fs curs_addch curs_mvaddstr
ln -fs curs_addch curs_refresh
ln -fs curs_addch curs_scroll
ln -fs curs_attroff curs_attron
ln -fs curs_attroff curs_attrset
ln -fs curs_attroff curs_standend
ln -fs curs_attroff curs_standout
ln -fs curs_cbreak curs_clearok
ln -fs curs_cbreak curs_echo
ln -fs curs_cbreak curs_get-magic-value
ln -fs curs_cbreak curs_idlok
ln -fs curs_cbreak curs_keypad
ln -fs curs_cbreak curs_nl
ln -fs curs_cbreak curs_nocbreak
ln -fs curs_cbreak curs_nodelay
ln -fs curs_cbreak curs_noecho
ln -fs curs_cbreak curs_nonl
ln -fs curs_cbreak curs_noraw
ln -fs curs_cbreak curs_raw
ln -fs curs_cbreak curs_resetty
ln -fs curs_cbreak curs_savetty
ln -fs curs_cbreak curs_scrollok
ln -fs curs_clear curs_clrtobot
ln -fs curs_clear curs_clrtoeol
ln -fs curs_color-set curs_has-colors
ln -fs curs_cursoff curs_curson
ln -fs curs_cursoff curs_mvcur
ln -fs curs_delch curs_deleteln
ln -fs curs_delch curs_insch
ln -fs curs_delch curs_insertln
ln -fs curs_delch curs_mvdelch
ln -fs curs_delch curs_mvinsch
ln -fs curs_endwin curs_initscr
ln -fs curs_flushinp curs_cols
ln -fs curs_flushinp curs_getch
ln -fs curs_flushinp curs_getyx
ln -fs curs_flushinp curs_inch
ln -fs curs_flushinp curs_lines
ln -fs curs_flushinp curs_mvgetch
ln -fs curs_flushinp curs_mvinch
ln -fs curs_flushinp curs_unctrl
ln -fs curs_flushinp curs_ungetch
ln -fs define-class call-next-method
ln -fs define-class class-of
ln -fs define-class classp
ln -fs define-class define-generic
ln -fs define-class define-method
ln -fs define-class initialize
ln -fs define-class instancep
ln -fs define-class make-instance
ln -fs define-class slot-ref
ln -fs define-class slot-setb
ln -fs define-matcher let-matcher
ln -fs define-matcher make-matcher
ln -fs draw-tree dt
ln -fs duplicates dupp
ln -fs duplicates dupq
ln -fs duplicates dupv
ln -fs eq ge
ln -fs eq gt
ln -fs eq le
ln -fs eq lt
ln -fs exact-to-inexact inexact-to-exact
ln -fs exactp inexactp
ln -fs exp log
ln -fs exp sin
ln -fs exp cos
ln -fs exp tan
ln -fs exp asin
ln -fs exp acos
ln -fs exp atan
ln -fs exponent mantissa
ln -fs floor ceiling
ln -fs floor round
ln -fs floor truncate
ln -fs force delay
ln -fs gcd lcm
ln -fs get-prop put-prop
ln -fs get-prop rem-prop
ln -fs get-prop put-propb
ln -fs get-prop rem-propb
ln -fs help apropos
ln -fs htmlify-char htmlify-string
ln -fs id false
ln -fs id true
ln -fs input-portp output-portp
ln -fs integer-to-binary-string binary-string-to-integer
ln -fs integer-to-binary-string number-of-bits
ln -fs iota iotastar
ln -fs keyword-value accept-keywords
ln -fs loutify-char loutify-string
ln -fs macro-expand macro-expand-1
ln -fs make-array array
ln -fs make-array arrayp
ln -fs make-array array-dimensions
ln -fs make-array array-map
ln -fs make-array array-rank
ln -fs make-array array-ref
ln -fs make-array array-setb
ln -fs make-array subarray
ln -fs make-canvas canvas-draw
ln -fs make-canvas canvas-draw-string
ln -fs make-canvas canvas-dump
ln -fs make-canvas canvas-plot
ln -fs make-canvas canvas-plot-line
ln -fs make-hash-table alist-to-hash-table
ln -fs make-hash-table hash-table-length
ln -fs make-hash-table hash-table-ref
ln -fs make-hash-table hash-table-removeb
ln -fs make-hash-table hash-table-setb
ln -fs make-hash-table hash-table-to-alist
ln -fs make-rbt rbt-find
ln -fs make-rbt rbt-insert
ln -fs make-rbt rbt-rebuild
ln -fs make-rbt rbt-remove
ln -fs make-stream append-streams
ln -fs make-stream filter-stream
ln -fs make-stream list-to-stream
ln -fs make-stream map-stream
ln -fs make-stream stream-any
ln -fs make-stream stream-eosp
ln -fs make-stream stream-extract
ln -fs make-stream stream-id
ln -fs make-stream stream-iota
ln -fs make-stream stream-member
ln -fs make-stream stream-next
ln -fs make-stream stream-none
ln -fs make-stream stream-to-list
ln -fs make-stream stream-value
ln -fs max min
ln -fs memoize define-memoizing
ln -fs memq member
ln -fs memq memv
ln -fs minus slash
ln -fs module using
ln -fs numberp integerp
ln -fs numberp realp
ln -fs parse-optionsb display-usage
ln -fs parse-optionsb opt-argp
ln -fs parse-optionsb opt-char
ln -fs parse-optionsb opt-type
ln -fs parse-optionsb opt-val
ln -fs parse-optionsb option
ln -fs permute permutestar
ln -fs plus star
ln -fs position posp
ln -fs position posv
ln -fs position posq
ln -fs pretty-print pp
ln -fs pretty-print pp-file
ln -fs pretty-print pp-loop
ln -fs pretty-print pp-string
ln -fs prolog factb
ln -fs prolog new-databaseb
ln -fs prolog predicateb
ln -fs prolog query
ln -fs pushb decb
ln -fs pushb incb
ln -fs pushb popb
ln -fs pushb set-varsb
ln -fs pushb swapb
ln -fs quasiquote unquote
ln -fs quasiquote unquote-splicing
ln -fs queue make-queue
ln -fs queue queue-emptyp
ln -fs queue queueb
ln -fs queue unqueueb
ln -fs queue unqueuestar
ln -fs quotient modulo
ln -fs quotient remainder
ln -fs r4rs-procedures r4rs-syntax-objects
ln -fs r4rs-procedures s9fes-syntax-objects
ln -fs r4rs-procedures s9fes-procedures
ln -fs r4rs-procedures s9fes-extension-procedures
ln -fs r4rs-procedures s9fes-extension-symbols
ln -fs random random-state
ln -fs re-comp re-match
ln -fs re-comp re-subst
ln -fs record assert-record-type
ln -fs record list-to-record
ln -fs record record-copy
ln -fs record record-equalp
ln -fs record record-ref
ln -fs record record-setb
ln -fs record record-signature
ln -fs record record-to-list
ln -fs record record-type-matches
ln -fs record recordp
ln -fs remove remp
ln -fs remove remq
ln -fs remove remv
ln -fs set-input-portb set-output-portb
ln -fs spawn-command spawn-command_fd
ln -fs spawn-shell-command spawn-shell-command_fd
ln -fs split-url url-anchor
ln -fs split-url url-args
ln -fs split-url url-host
ln -fs split-url url-path
ln -fs split-url url-proto
ln -fs split-url url-suffix
ln -fs standard-error-port call-with-stderr
ln -fs standard-error-port with-output-to-stderr
ln -fs starstar starepsilonstar
ln -fs starstar starextensionsstar
ln -fs starstar starhost-systemstar
ln -fs starstar starlibrary-pathstar
ln -fs starstar starloadingstar
ln -fs string-find string-ci-find
ln -fs string-find string-ci-find-word
ln -fs string-find string-find-word
ln -fs string-find-last string-ci-find-last
ln -fs string-find-last string-ci-find-last-word
ln -fs string-find-last string-find-last-word
ln -fs string-last-position string-ci-last-position
ln -fs string-last-position string-ci-last-word-position
ln -fs string-last-position string-last-word-position
ln -fs string-locate string-ci-locate
ln -fs string-map string-mapb
ln -fs string-position string-ci-position
ln -fs string-position string-ci-word-position
ln -fs string-position string-word-position
ln -fs string-prefixeqp string-prefix-cieqp
ln -fs string-scan string-ci-scan
ln -fs string-to-list list-to-string
ln -fs string-upcase string-downcase
ln -fs stringeqp string-cieqp
ln -fs stringltp string-cigep
ln -fs stringltp string-cigtp
ln -fs stringltp string-cilep
ln -fs stringltp string-ciltp
ln -fs stringltp stringgep
ln -fs stringltp stringgtp
ln -fs stringltp stringlep
ln -fs sys_chmod sys_change_mode
ln -fs sys_dup sys_dup2
ln -fs sys_getgrnam sys_getgrgid
ln -fs sys_getpgid sys_setpgid
ln -fs sys_getpwnam sys_getpwuid
ln -fs sys_getuid sys_getgid
ln -fs sys_group-name sys_group-gid
ln -fs sys_inet-listen sys_inet-accept
ln -fs sys_kill sys_notify
ln -fs sys_make-input-port sys_make-output-port
ln -fs sys_open sys_close
ln -fs sys_open sys_creat
ln -fs sys_read sys_write
ln -fs sys_setuid sys_setgid
ln -fs sys_stat sys_lstat
ln -fs sys_stat-name sys_lstat-atime
ln -fs sys_stat-name sys_lstat-ctime
ln -fs sys_stat-name sys_lstat-dev
ln -fs sys_stat-name sys_lstat-gid
ln -fs sys_stat-name sys_lstat-ino
ln -fs sys_stat-name sys_lstat-mode
ln -fs sys_stat-name sys_lstat-mtime
ln -fs sys_stat-name sys_lstat-name
ln -fs sys_stat-name sys_lstat-nlink
ln -fs sys_stat-name sys_lstat-size
ln -fs sys_stat-name sys_lstat-uid
ln -fs sys_stat-name sys_stat-atime
ln -fs sys_stat-name sys_stat-ctime
ln -fs sys_stat-name sys_stat-dev
ln -fs sys_stat-name sys_stat-gid
ln -fs sys_stat-name sys_stat-ino
ln -fs sys_stat-name sys_stat-mode
ln -fs sys_stat-name sys_stat-mtime
ln -fs sys_stat-name sys_stat-nlink
ln -fs sys_stat-name sys_stat-size
ln -fs sys_stat-name sys_stat-uid
ln -fs sys_stat-regularp sys_lstat-block-devp
ln -fs sys_stat-regularp sys_lstat-char-devp
ln -fs sys_stat-regularp sys_lstat-directoryp
ln -fs sys_stat-regularp sys_lstat-pipep
ln -fs sys_stat-regularp sys_lstat-regularp
ln -fs sys_stat-regularp sys_lstat-socketp
ln -fs sys_stat-regularp sys_lstat-symlinkp
ln -fs sys_stat-regularp sys_stat-block-devp
ln -fs sys_stat-regularp sys_stat-char-devp
ln -fs sys_stat-regularp sys_stat-directoryp
ln -fs sys_stat-regularp sys_stat-pipep
ln -fs sys_stat-regularp sys_stat-socketp
ln -fs sys_user-name sys_user-gecos
ln -fs sys_user-name sys_user-gid
ln -fs sys_user-name sys_user-home
ln -fs sys_user-name sys_user-shell
ln -fs sys_user-name sys_user-uid
ln -fs sys_usleep sys_sleep
ln -fs sys_wait sys_waitpid
ln -fs t-sort t-sort-net
ln -fs tagbody go
ln -fs thread-create thread-exit
ln -fs thread-create thread-start
ln -fs thread-create thread-yield
ln -fs time-add time-subtract
ln -fs time-add time-difference
ln -fs time-add time-beforep
ln -fs time-add time-afterp
ln -fs time timestar
ln -fs type-case type-of
ln -fs vector-to-list list-to-vector
ln -fs vector-map vector-mapb
ln -fs when unless
ln -fs while until
ln -fs with-input-from-file with-output-to-file
ln -fs write-to-string display-to-string
ln -fs zerop evenp
ln -fs zerop negativep
ln -fs zerop oddp
ln -fs zerop positivep
