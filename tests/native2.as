const int iterations = 10000000;

void main()
{
    int length = 0;
    int i;
    for (i = 0; i < iterations; ++i)
        length = StrLen(s);


    PrintString("Length: ");
    PrintInt(iterations * length);
    PrintString("\n");
}
